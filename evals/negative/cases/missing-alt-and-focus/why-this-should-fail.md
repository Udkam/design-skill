# Why This Should Fail

The image has no alt text, the icon-only button has no accessible name, the input has no label or accessible name, and the interface has interactive controls without visible focus styling.
