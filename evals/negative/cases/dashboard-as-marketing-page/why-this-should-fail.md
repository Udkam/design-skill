# Why This Should Fail

The fixture is named as a dashboard case but contains a marketing hero and generic benefit cards instead of dense status, exceptions, operational state, or comparison workflows.
